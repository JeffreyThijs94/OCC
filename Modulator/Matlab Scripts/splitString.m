%Splits input string to array of characters
function [ out ] = splitString( string )

    for i=1:length(string)
            
        disp(string(i))
            
    %writePWMDutyCycle(a, pin, 200);
    end
end

